services:
  - type: web
    name: roblox-global-chat-api  # 好きなサービス名に変更可能です
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
